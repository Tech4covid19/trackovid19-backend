/**
 * Enum for case conditions
 */

const CONDITIONS = {
    NORMAL: 'normal',
    INFECTED: 'infected',
    QUARANTINE: 'quarantine',
    SYMPTOMATIC: 'symptomatic',
    RECOVERED: 'recovered'
};

module.exports = Object.freeze(CONDITIONS);
